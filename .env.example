# Port du backend Fastify
PORT=3001

# Finnhub (actions) — clé gratuite sur https://finnhub.io
# Sans clé, les actions sont désactivées ; la crypto fonctionne quand même (Binance public).
FINNHUB_API_KEY=

# Origine autorisée pour le dashboard (CORS)
WEB_ORIGIN=http://localhost:5173
